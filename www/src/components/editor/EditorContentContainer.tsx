import { chakra } from "@chakra-ui/react";

export const EditorContentContainer = chakra("div", {
	label: "EditorContentContainer",
	baseStyle: {
		flex: "1 0 200px",
		outline: "none",
		outlineOffset: 0,
	},
});
